# One of the OOP way to call and modify a self-class instances

class Solution:
    def twoSum(self, nums: list[int], target: int) -> list[int]:
        pass

# As far as I know, this self-calling parameter 
# is to replace the __init__ function
# We can still use instance inside the class itself